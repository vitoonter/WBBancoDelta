import React from 'react'

const LoginLogo = ({text, aditional_text}) => (
  <div className="col-md-12">
    <img src={logoLogin} className="center" alt="logo"></img>
  </div>
);

export default LoginLogo